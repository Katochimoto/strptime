#!/usr/bin/env node

var nopt = require('nopt');
var strptime = require('../strptime');

var opts = {
    "s": [String],
    "f": [String, null],
    "u": [Boolean, null]
};

var args = nopt(opts, {}, process.argv, 2);

if (args.s) {
    console.log(strptime(args.s, args.f, args.u));
}